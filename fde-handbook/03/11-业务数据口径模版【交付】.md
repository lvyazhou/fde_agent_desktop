# 业务数据口径模版

> FDE 阶段③ 交付物模板（通用）。用于把一个数据产品里**每一个指标**的取数出处、计算逻辑、去重/过滤/优先级规则一次性定死，作为阶段④建模、阶段⑤验收的对账依据。
>
> **适用形态**：看板（实时 / 大屏指标）、报告（日 / 周 / 月 / 季 / 年周期汇总）、推送报告（阈值 / 告警 / 事件触发）。
>
> **核心要求**：每个指标必须写清**从哪个库、哪张表、哪些字段取数**，并把信息科提供的 **SQL 原样贴到「取数SQL」列**——这是本交付物的价值所在，阶段④建模直接照此对齐物理表。
>
> **使用方法**：删掉下面所有 `示意` 行，按本项目实际业务逐条填写。无法确定的口径 / 未拿到 SQL 的先**置空、标记「待信息科提供」**，不要编造表名或 SQL。

---

## 一、全局口径约定（本次交付统一遵循）

先把跨所有指标的通用规则定死，指标明细表里就不必逐条重复。按项目实际填写，用不到的整条删除。

- **数据源清单**：本项目涉及哪些库 / schema（如 `his`、`emr`、埋点库 `dwd`），各自的连接方 / 责任人（信息科对接人），哪些必备、哪些可选。
- **命名与血缘**：明细表「数据来源表」列统一写成 `库.表(关键字段)`，多表 join 的写主表并在「取数SQL」列体现关联；确保每个指标可追溯到物理字段。
- **过滤逻辑**：全局默认过滤掉哪些数据（如：测试数据、`status` 无效、软删除 `is_deleted=1`、低于某等级）。
- **去重逻辑**：同一实体多条记录时按什么唯一键去重、保留哪一条（最新 / 最早 / 优先级最高）。
- **优先级 / 归并**：同一实体在多源命中或多条记录合并时，以哪一条为准。
- **时间口径**：统计周期按什么时间字段切（创建时间 / 发生时间 / 完成时间），时区与截断到日/时的规则。
- **单位与精度**：金额单位、百分比小数位、计数是否含 0 等展示口径。
- **标记约定**：模型自动生成的内容与需人工补全的内容各用什么方式标记（如生成标绿、待补标紫），交付前待补项须清零。

---

## 二、指标口径明细表

按 `章 / 节` 分组，逐行填清。**指标类型**分「基础指标」（直接取字段）与「衍生指标」（需按逻辑二次计算）。**「数据来源表」写 `库.表(字段)`，「取数SQL」贴信息科给的可执行 SQL。** 下方 `示意` 行仅示范格式与颗粒度，交付时删除、换成本项目真实指标与真实 SQL。

| 章 | 节 | 指标 | 指标类型 | 指标口径 | 数据来源表 | 取数SQL | 逻辑 | 示例 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 概览 | 基本信息 | 客户名称 | 基础指标 | 直接取客户主数据名称 | `crm.customer(name)` | `SELECT name FROM crm.customer WHERE id = :cid` | 无需计算 | 示意 · XX 公司 |
| 概览 | 基本信息 | 统计周期 | 基础指标 | 按任务配置取起止日期 | `job.report_task(begin_date,end_date)` | `SELECT begin_date, end_date FROM job.report_task WHERE task_id = :tid` | 取工单配置 | 示意 · 2026-08-01 ~ 2026-08-31 |
| — 看板（实时 / 大屏指标） — |||||||||
| 看板 | 核心指标卡 | 今日活跃数 | 衍生指标 | 当日去重后的活跃实体数 | `dwd.user_action(uid,action_time)` | `SELECT count(DISTINCT uid) FROM dwd.user_action WHERE action_time >= CURRENT_DATE` | 按 uid 去重；当日 0 点至今 | 示意 · 1,240 |
| 看板 | 核心指标卡 | 实时处理率 | 衍生指标 | 已处理 / 总量 | `ticket.work_order(status)` | `SELECT sum(status='done')/count(*)::numeric FROM ticket.work_order WHERE created_at >= CURRENT_DATE` | 保留 1 位小数，分母 0 显示「-」 | 示意 · 92.4% |
| 看板 | 趋势图 | 近 7 日走势 | 衍生指标 | 按天聚合的核心指标序列 | `dwd.user_action(action_time)` | `SELECT date(action_time) d, count(*) FROM dwd.user_action WHERE action_time >= CURRENT_DATE-6 GROUP BY d` | 补齐无数据日期为 0 | 示意 · 折线 7 点 |
| — 报告（周期汇总） — |||||||||
| 报告 | 汇总头 | 本期总量 | 衍生指标 | 报告周期内的总计数 | `ticket.work_order(id,created_at)` | `SELECT count(DISTINCT id) FROM ticket.work_order WHERE created_at BETWEEN :begin AND :end` | 套用全局过滤 | 示意 · 8,610 |
| 报告 | 汇总头 | 环比变化 | 衍生指标 | 本期相对上期的增减幅度 | 同上（本期 / 上期两次取数） | `-- 本期与上期各跑一次总量，(本期−上期)/上期` | 上期为 0 标「新增」 | 示意 · +12.3% |
| 报告 | 评级 | 综合评级 | 衍生指标 | 按评分规则得出的等级 | 多指标汇总（见各指标 SQL） | `-- 汇总各扣分项后按分档映射：优≥90/良≥80/一般≥70…` | 定义每项加减分口径 | 示意 · 良好 |
| — 推送报告（告警 / 事件触发） — |||||||||
| 推送 | 触发条件 | 触发阈值 | 衍生指标 | 指标达到条件时触发推送 | `event.alarm(level,tag,occur_time)` | `SELECT * FROM event.alarm WHERE level='high' AND occur_time >= :since` | 阈值 + 去抖/冷却规则 | 示意 · 高危事件 ≥ 1 |
| 推送 | 推送内容 | 事件明细 | 基础指标 | 触发时携带的关键字段 | `event.alarm(occur_time,object,level,advice)` | `SELECT occur_time,object,level,advice FROM event.alarm WHERE id = :aid` | 推送体字段清单 | 示意 · 5 条 |
| 推送 | 推送对象 | 接收人 | 基础指标 | 按规则确定接收人 | `sys.route_rule(role,owner)` | `SELECT receiver FROM sys.route_rule WHERE biz_type = :bt` | 按角色/责任人/订阅路由 | 示意 · 运维组 |
| — 医疗行业示意（看板 / 报告 / 推送，可整组删除或替换） — |||||||||
| 看板 | 门诊运营 | 当日门诊量 | 衍生指标 | 当日实际就诊人次 | `his.outp_regist(visit_id,visit_date,status)` | `SELECT count(DISTINCT visit_id) FROM his.outp_regist WHERE visit_date = CURRENT_DATE AND status = 1` | 退号/未到（status≠1）不计 | 示意 · 3,820 人次 |
| 看板 | 住院运营 | 床位使用率 | 衍生指标 | 实际占用床日 / 开放床日 | `his.inp_bed_day(occupied_days,open_days)` | `SELECT sum(occupied_days)/sum(open_days)::numeric FROM his.inp_bed_day WHERE stat_date = CURRENT_DATE` | 保留 1 位小数 | 示意 · 96.2% |
| 看板 | 急诊 | 急诊留观人数 | 基础指标 | 当前留观区在观患者数 | `his.emerg_observe(patient_id,obs_status)` | `SELECT count(*) FROM his.emerg_observe WHERE obs_status = 'in'` | 实时取在观状态 | 示意 · 42 人 |
| 报告 | 医疗质量 | 平均住院日 | 衍生指标 | 出院者占用总床日 / 出院人数 | `his.inp_discharge(los_days,discharge_date)` | `SELECT sum(los_days)/count(*)::numeric FROM his.inp_discharge WHERE discharge_date BETWEEN :begin AND :end` | 跨月按出院日归期 | 示意 · 7.8 天 |
| 报告 | 医疗质量 | 药占比 | 衍生指标 | 药品收入 / 医疗总收入 | `his.income_detail(item_type,amount)` | `SELECT sum(amount) FILTER(WHERE item_type='drug')/sum(amount)::numeric FROM his.income_detail WHERE stat_month = :ym` | 含精度口径 | 示意 · 28.6% |
| 报告 | 病案 | DRG 入组率 | 衍生指标 | 成功入组病例 / 应入组病例 | `emr.mr_home(drg_code,discharge_date)` | `SELECT count(*) FILTER(WHERE drg_code IS NOT NULL)/count(*)::numeric FROM emr.mr_home WHERE discharge_date BETWEEN :begin AND :end` | 未入组（空白/歧义）计入分母 | 示意 · 99.1% |
| 报告 | 患者服务 | 患者满意度 | 衍生指标 | 满意问卷数 / 有效回收问卷数 | `svc.satisfaction(score,valid,submit_date)` | `SELECT count(*) FILTER(WHERE score>=4)/count(*) FILTER(WHERE valid=1)::numeric FROM svc.satisfaction WHERE submit_date BETWEEN :begin AND :end` | 定义有效问卷口径 | 示意 · 94.5% |
| 推送 | 临床预警 | 危急值触发 | 衍生指标 | 检验结果命中危急值区间时推送 | `lis.test_result(item_code,result_value,report_time)` | `SELECT * FROM lis.test_result WHERE item_code=:code AND result_value >= :crit_high AND report_time >= :since` | 超阈即触发；已处置不再推 | 示意 · 血钾 ≥ 6.5 |
| 推送 | 感控 | 院感事件 | 衍生指标 | 疑似院内感染达标即推送 | `nis.infection_event(ward_id,source,occur_time)` | `SELECT ward_id, count(*) FROM nis.infection_event WHERE occur_time >= :since GROUP BY ward_id HAVING count(*) >= 3` | 同病区同源 ≥ N 例触发 | 示意 · 同病区 ≥ 3 例 |
| 推送 | 药事 | 高危医嘱拦截 | 基础指标 | 触发拦截时携带的关键字段 | `cpoe.order_intercept(patient_id,order_id,rule,doctor)` | `SELECT patient_id,order_id,rule,doctor FROM cpoe.order_intercept WHERE intercept_time >= :since` | 推送体字段清单 | 示意 · 3 条 |

---

## 三、指标类型说明

- **基础指标**：直接来源于配置或单表原始字段，无需二次计算（如客户名称、报告周期、接收人）。
- **衍生指标**：需按「取数SQL」+「逻辑」二次计算得出（如活跃数、环比、平均住院日、DRG 入组率、触发阈值）。

## 四、填写与验收要求

- **每个指标必须写清「数据来源表」（`库.表(字段)`）与「取数SQL」**；SQL 直接采用信息科提供的版本，参数用 `:begin`、`:tid` 等占位便于阶段④套用。
- 「示例」列填**本项目真实样例值**，供业务方核对，不留占位符。
- 表中所有 `示意`、`—…—` 分隔行在交付前全部删除；未拿到 SQL / 待补的口径按全局约定标记，交付前清零。
- 阶段④按「数据来源表」对齐物理建模、按「取数SQL」实现指标；阶段⑤按本表逐指标验收对账。
