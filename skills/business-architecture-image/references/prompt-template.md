# 庄重商务架构图 Prompt 模板

将花括号内容替换为用户提供的信息，未提供的内容不要自行虚构。

```text
白色背景，庄重大方严肃商务风格，适合医院信息化、信息科和院领导正式汇报。全图只使用深蓝、藏青、钢蓝、浅灰蓝和白色，不使用红色、黄色、橙色、金色、紫色、暖色、霓虹光和发光渐变。画布比例16:9，内容均匀填充整个画布，左右视觉重量平衡，无大块空白。

顶部居中完整标题：{标题}
标题下方副标题：{副标题}
标题不得缺失、改写、缩写或移位。

画面采用左侧三层独立卡片式架构、右侧三道安全防线的正式咨询报告布局。左侧设置一条贯穿三层的深蓝纵向主轴，编号1、2、3清晰标注，主数据流自底向上。三张层级卡片大小接近、间距一致、边缘对齐，不使用简单横色带。

第一层顶部卡片：工作台层。
必须明确画出一个包裹整个工作台区域的蓝色虚线大框，左上角清楚标注“360私有云”。
虚线框左侧：{行业工作台和业务方向}
虚线框右侧：{智能体引擎和运行边界}
安全标签：{业务数据阅后即焚等要求}

第二层中部卡片：SDR数据基座。
服务网关层：{模块}
服务平台层：{模块}
数据治理层：{模块}
三个子模块等宽、对齐、内容密度均衡。

第三层底部卡片：数据层。
左侧数据源：{Oracle/HIS/LIS/PACS/EMR等}
右侧数据源：{HR MySQL等}
旁路规则：{旁路进入服务平台层脱敏}
左右两个数据源卡片等高、内容均衡。

右侧三道安全防线为三个等高独立卡片：
数据安全：{内容}
大模型安全：{内容}
智能体安全：{内容}

底部承诺横条：{核心承诺}

视觉风格：企业级商务信息图、医院正式汇报材料、白底深蓝系、卡片整齐、线条清楚、模块等宽、间距统一、层级端正、严谨可信、克制不花哨。禁止人物、营销海报、赛博朋克、游戏UI、复杂纹理、彩色强调和装饰性光效。所有文字使用中文标签。
```

## 默认命令

```bash
PYTHONIOENCODING=utf-8 python C:/Users/lvyazhou/.product-lobster/skills/image-generator/generate_images.py \
  -p "{完整提示词}" \
  -s 1792x1024 -q high \
  -d "{输出目录}" -o "{名称}_商务vN.png"
```
